[package]
name = "regorus-profile"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.8", features = ["derive"] }
regorus = { version = "0.2.1", default-features = false, features = ["std", "regex", "semver"] }
serde_json = "1.0.120"
